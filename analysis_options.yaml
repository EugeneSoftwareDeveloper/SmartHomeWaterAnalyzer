# Strict analyzer для production-качества Flutter-кода.
# Базируется на flutter_lints + расширенные правила.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Возводим до error то, что обычно warning — чтобы build падал, а не молча копил долг.
    missing_required_param: error
    missing_return: error
    invalid_annotation_target: ignore  # freezed может ругаться, не критично

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gr.dart"
    - "**/generated_plugin_registrant.dart"
    - "lib/l10n/generated/**"

linter:
  rules:
    # Стиль и форматирование.
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_pub_dependencies: false  # держим в логическом порядке, не алфавите
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    use_key_in_widget_constructors: true

    # Безопасность.
    avoid_dynamic_calls: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    only_throw_errors: true
    throw_in_finally: true

    # Производительность.
    avoid_function_literals_in_foreach_calls: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true

    # Дизайн API.
    avoid_classes_with_only_static_members: false  # для каталогов норм/команд удобно
    prefer_relative_imports: true
    public_member_api_docs: false  # для приложения, не библиотеки

    # Читаемость.
    always_declare_return_types: true
    avoid_unused_constructor_parameters: true
    leading_newlines_in_multiline_strings: true
    parameter_assignments: true
    use_string_in_part_of_directives: true

    # BLE-специфика.
    avoid_void_async: true
