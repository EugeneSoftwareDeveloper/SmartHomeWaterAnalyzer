# SmartHomeWaterAnalyzer

[![CheckIn](https://github.com/EugeneSoftwareDeveloper/SmartHomeWaterAnalyzer/actions/workflows/ci.yml/badge.svg)](https://github.com/EugeneSoftwareDeveloper/SmartHomeWaterAnalyzer/actions/workflows/ci.yml)

Android-приложение для тестера качества воды **YINMIK BLE-C600** (и совместимых white-label вариантов семейства BLE-YC01) / 7 в 1 Temp ORP EC TDS минерализация S.G измеритель PH онлайн тестер качества воды с голубыми зубьями приложение для контроля питьевой лаборатории аквариума. 
Подключается к прибору по Bluetooth LE напрямую с телефона, читает текущие показания, отображает их с цветными шкалами «опасно / норма / отлично» — чтобы не сравнивать цифры с бумажной таблицей вручную.

<img width="380" height="720" alt="image" src="https://github.com/user-attachments/assets/e3d270b8-d367-4def-b045-de252d06b19e" />


## Что умеет

- **Сканирование BLE и подключение** к YINMIK BLE-C600. Фильтр по имени на клиенте + диагностика «BLE-устройств в эфире: N» + bottom-sheet со всеми видимыми BLE-устройствами для случаев, когда прибор называется неожиданно.
- **Быстрое переподключение** — карточка «Подключиться к BLE-C600» на главном экране подключается к последнему прибору напрямую, без сканирования: открыл приложение → один тап → показания.
- **Чтение 7 параметров** с цветными шкалами, развёрнутыми названиями («Кислотность (pH)», «Минерализация (TDS)» и т.д.) и сводной оценкой воды.
- **Сравнение с прошлым замером** — под каждым значением дельта к предыдущему замеру **в том же месте**: «↑ +0.12», «↓ −45» или «без изменений». Цветом отмечена только смена зоны качества (зелёный — стало лучше, красный — хуже): движение внутри одной зоны и сдвиг меньше погрешности прибора не подкрашиваются, чтобы дрожание электрода не выглядело результатом.
- **Справка по каждому параметру** с тонкой градацией (5–9 уровней): тап по карточке → подробное описание с конкретными числовыми диапазонами и человеко-понятными заметками.
- **Управление прибором**: переключатели подсветки и HOLD (спекулятивные байты — см. инструкцию ниже).
- **Debug-страница для подбора команд**: 14 пресетов байтов + ручной hex-ввод. Тап по пресету отправляет байты и сразу сверяет бит статуса в кадре FF02 до и после записи; результат виден в логе попыток. Открывается из шапки экрана показаний.
- **Локальная история измерений** в SQLite с **ручным сохранением** — FAB «Сохранить замер» на экране показаний; случайные/тестовые чтения историю не засоряют. Кнопка показывает состояние «Сохранено» с чек-иконкой; SnackBar даёт 5 секунд на отмену.
- **Места замеров устроены иерархией** «место → комната → источник»: дом и дача — разные места, внутри них комнаты, а в комнате может быть несколько источников (на кухне уживаются фильтр и аквариум). Комната необязательна — «Дача · Скважина» валидный адрес. Управление в настройках → «Места замеров».
- **Место подставляется по координатам.** Придя на дачу, вы видите её адрес уже выбранным, с подписью «определено по координатам». Координаты место узнаёт само из ваших же замеров; можно привязать и вручную. Автовыбор не запрашивает разрешение и не перебивает выбор, сделанный руками.
- **Места замера** (до версии 1.4.0) — выбирались из списка вместо ручного ввода: девять готовых вариантов (кран на кухне, после фильтра, кулер, бутилированная, скважина, колодец, родник, аквариум, бассейн), можно добавить свои. Недавно использованные поднимаются наверх. Место существующей записи редактируется из детального просмотра.
- **Геометка замеров** — координаты сохраняются вместе с замером, в детальном просмотре есть кнопка «На карте» (открывает Яндекс.Карты, Google Maps, OsmAnd или 2ГИС). Отключается в настройках; отсутствие GPS или отказ в разрешении не мешают сохранить замер.
- **Хронология истории** — записи группируются по дню (Сегодня / Вчера / `dd.MM.yyyy`).
- **Удаление записей** — swipe-to-delete в списке с undo-SnackBar, либо «Удалить замер» в меню детального просмотра. Восстановление в течение 5 секунд возвращает запись с тем же id.
- **Детальный просмотр со свайпом** — тап по записи в истории показывает все карточки параметров, свайп влево/вправо листает между замерами для сравнения.
- **Графики во времени** — переключатель параметра над диаграммой (pH / ORP / EC / TDS / Salinity / Temp / S.G.) и фильтр по месту, последние 50 точек, диапазон y-оси берётся из шкалы параметра.
- **Экспорт CSV** — вся история одним тапом, с адресом в трёх колонках (место, комната, источник) и координатами, открывается через системный share-sheet.
- **Профили норм**: питьевая вода / бассейн / аквариум (пресный) / гидропоника — переключаются в настройках, влияют на цветные зоны и текст в справке. Профиль запоминается в самом замере, поэтому переключение настройки не меняет оценку уже сохранённых записей.
- **Тёмная тема** — по системе, или принудительно в настройках.
- **Локальные уведомления** при выходе параметров из нормы (опционально).
- **Русский и английский интерфейс** — язык определяется по системе, и его можно выбрать вручную в настройках. Переведены все экраны, включая подробную справку по параметрам. Стартовый набор мест и источников создаётся сразу на языке пользователя: замер хранит имя источника, и переводить его задним числом уже нельзя.
- **Реакция на выключение Bluetooth** — отдельный экран с кнопкой «Включить Bluetooth».
- **История доступна без подключения** — открывается из шапки главного экрана.

## Что показывает

7 параметров качества воды + статус прибора:

| Параметр | Единица | Что значит |
|---|---|---|
| **pH** | — | Кислотность/щёлочность. Норма питьевой воды 6.5–8.5 |
| **ORP** | мВ | Окислительно-восстановительный потенциал |
| **EC** | µС/см | Электропроводность |
| **TDS** | ppm | Общая минерализация |
| **Соленость** | ppm | Соленость в ppm |
| **Температура** | °C | Температура воды |
| **S.G.** | — | Удельная плотность |
| Батарея | мВ + % | Состояние батареи прибора (оценка) |

Каждый параметр — на отдельной карточке с **крупным значением** и **цветной шкалой**, на которой:
- цветные сегменты обозначают зоны качества для **питьевой воды** (ВОЗ/СанПиН-ориентир);
- треугольный маркер показывает текущее значение и **анимируется** при обновлении;
- бейдж в шапке карточки показывает текущую зону (`Опасно` / `Внимание` / `Приемлемо` / `Норма` / `Оптимум`);
- тап по карточке раскрывает описание и пояснение нормы.

Сверху — **сводная hero-карточка**: общая оценка воды (по худшему параметру) + батарея прибора + чипы HOLD/LIGHT, если активны на приборе.

Снизу — **управление**: переключатели подсветки и HOLD (см. [Управление](#4-управление-прибором) ниже).

## Требования

| Что нужно | Зачем |
|---|---|
| **Прибор YINMIK BLE-C600** или совместимый (BLE-YC01 family) | источник данных |
| **Android 7.0+** с поддержкой Bluetooth LE | целевая платформа |
| Закрытое официальное приложение YINMIK | прибор держит **только одно** BLE-подключение одновременно |

> **Важно про BLE-подключение**: BLE-C600 не позволяет двум клиентам подключиться к нему одновременно. Если у тебя на телефоне открыто официальное приложение YINMIK и оно подключено к прибору — наше приложение не найдёт его (или найдёт, но не сможет подключиться). Полностью закрой официальное приложение (force stop) перед использованием.

## Быстрый старт

### Установка APK из GitHub Releases

1. Открой страницу [Releases](https://github.com/EugeneSoftwareDeveloper/SmartHomeWaterAnalyzer/releases) и скачай APK для своей архитектуры:
   - **`water-analyzer-X.Y.Z-arm64-v8a.apk`** — большинство современных Android-телефонов (2018+). Если не уверен — бери этот.
   - `water-analyzer-X.Y.Z-armeabi-v7a.apk` — старые 32-битные устройства.
   - `water-analyzer-X.Y.Z-x86_64.apk` — для эмуляторов на ПК.
2. На телефоне разреши установку из неизвестных источников (Settings → Apps → Special access → Install unknown apps).
3. Установи APK, открой приложение.
4. Разреши Bluetooth. Геолокацию Android попросит на версиях до 11 включительно — без неё там не работает BLE-сканирование.
5. На приборе — длинное нажатие `ON/OFF` для включения Bluetooth-режима.
6. В приложении → «Сканировать» → выбери прибор → смотри показания.

> **Подпись APK**: если в репозитории не настроены release-secrets (см. ниже), релизные APK подписаны debug-keystore'ом. Это нормально для установки «скачал → поставил», но при апгрейде Android требует ту же подпись — устанавливай новые версии поверх старых без удаления.

> **Google Play Защита может молча блокировать установку**: диалог «Приложение заблокировано для защиты устройства» с итогом «Приложение не установлено» — даже после «Всё равно установить». Это нормальная реакция Play Protect на sideload-APK от неизвестного ей разработчика. Обход: открой **Google Play → аватар профиля → Play Защита → ⚙ настройки → выключи «Сканирование приложений»**, установи APK, затем **включи защиту обратно** — установленное приложение продолжит работать, а обновления той же подписью проходят легче.

### Сборка из исходников

См. [Сборка](#сборка) ниже.

## Использование

### 1. Подготовка прибора

Включи BLE на BLE-C600 длинным нажатием `ON/OFF` (до появления BLE-индикатора). Убедись, что **официальное приложение YINMIK не подключено** к прибору.

### 2. Сканирование

Открой Water Analyzer → автоматически начнётся сканирование. Прибор появится в списке как `BLE-C600` или похожее. Тапни на него.

При первом запуске Android попросит разрешения на Bluetooth (на 12+) или геолокацию (на старых версиях) — нужно разрешить.

Остальные разрешения приложение просит только в момент, когда они нужны, и отказ ничего не ломает:

- **Геолокация** — при первом сохранении замера с включённой геометкой. Откажете — замер сохранится без координат.
- **Уведомления** (Android 13+) — при первом замере вне нормы, если уведомления о выходе из нормы включены в настройках.

### 3. Чтение показаний

После подключения сразу читается один кадр. На экране — hero-карточка с общей оценкой + 7 карточек параметров. Вытяни вниз (pull-to-refresh) или нажми ⟳ в правом верхнем углу для нового чтения.

Под шапкой — строка «Сравнение с замером …»: она говорит, с каким именно прошлым замером сопоставлены дельты на карточках. База берётся по выбранному месту, поэтому смена места в поле выше сразу меняет и сравнение. Если в этом месте ещё не сохраняли, вместо неё будет «Первый замер в этом месте».

### 4. Управление прибором

Внизу экрана — секция «Управление прибором» с переключателями:

- **Подсветка** — включает/выключает подсветку экрана прибора. Состояние отражает реальный бит 0x08 статус-байта FF02. После переключения приложение автоматически перечитывает кадр для подтверждения.
- **Удержание показаний (HOLD)** — фиксирует текущие значения на экране прибора. Бит 0x10 статус-байта.

> ⚠️ **Команды управления — спекулятивные.** Производитель не публикует протокол. Текущие байты в `lib/yinmik/commands.dart` — обоснованная догадка (single byte = бит статуса). На большинстве партий BLE-C600 это должно работать; если не работает на твоей — см. [docs/03-control-commands.md](./docs/03-control-commands.md), там пошаговая инструкция как добыть точные байты через HCI snoop log с официального приложения.

### 5. Сохранение замера и адрес

Над карточками — поле **«Где мерим»**: место, комната и источник, например «Дом · Кухня · Фильтр». Если место уже привязано к координатам, адрес подставится сам, с подписью «определено по координатам»; тап по полю — выбрать другой. Выбранный вручную адрес подстановка до конца сессии не трогает.

Места, комнаты и источники настраиваются в **Настройках → Места замеров**. Там же место можно привязать к текущей точке, если в нём ещё не сохраняли замеров, — в остальных случаях координаты место узнаёт само.

Замер попадает в историю только по кнопке **«Сохранить замер»**; сразу после сохранения его можно отменить кнопкой в подсказке внизу экрана.

### 6. История и настройки

Вкладка «История» — график выбранного параметра с фильтром по адресу и список замеров по дням. Свайп влево удаляет запись (с отменой), тап открывает подробности: там можно сменить адрес и открыть точку на карте. Экспорт CSV — в меню ⋮. История доступна и без подключения к прибору — из шапки главного экрана.

В настройках — профиль норм, тема, **язык интерфейса** (по умолчанию — как в системе), уведомления о выходе из нормы и геометка замеров.

## Сборка

### Что нужно установить

1. [Flutter SDK 3.47.2](https://docs.flutter.dev/get-started/install/windows) — та же версия, что запинена в CI (вместе тянет Dart SDK)
2. Android SDK (через Android Studio или standalone command-line tools)
3. Включённый «режим разработчика» и USB-debugging на телефоне (или Android-эмулятор с BLE)

Проверь готовность:

```powershell
flutter doctor
```

### Dev-запуск (с подключённым телефоном)

```powershell
cd D:\AI\SmartHomeWaterAnalyzer
flutter pub get
flutter run
```

Hot reload работает: правки в Dart-коде применяются без переустановки.

### Release APK

```powershell
flutter build apk --release --split-per-abi
```

Флаг `--split-per-abi` обязателен: в `android/app/build.gradle.kts` включён ABI split без универсального APK, и без флага сборка падает, не найдя fat-APK. Готовые файлы — в `build/app/outputs/flutter-apk/`: `app-arm64-v8a-release.apk` подходит большинству телефонов. Скопируй на телефон и установи.

### Тесты и анализ

```powershell
flutter analyze   # статический анализ (0 issues должны быть)
flutter test      # unit- и widget-тесты
```

### Инструменты в `tools/`

Четыре скрипта, каждый закрывает свой способ узнать о проблеме слишком поздно.

| Инструмент | Что делает |
|---|---|
| `ship.py` | Прогоняет **те же** проверки, что `ci.yml`, локально, и только потом ставит тег. Релиз падает через десять минут почти всегда не на сборке, а на мелочи: незамеченный `info` анализатора, забытый `build_runner`, неотформатированный файл, версия в pubspec, разошедшаяся с тегом. Дополнительно сверяет три вещи, которые workflow проверить не может, не потратив те же десять минут: версия совпадает с тегом, раздел CHANGELOG существует и непуст, дерево чистое. |
| `preview/render_preview.dart` | Рисует карточки параметров и шкалы в PNG, в обеих темах сразу, без устройства и без APK. Половина интерфейса здесь — цвет: зоны шкалы, окраска карточки, знак и цвет дельты. По коду это не проверить, а собирать APK ради взгляда — минуты. |
| `check_so_alignment.py` | Проверяет выравнивание нативных библиотек на 16 KB — требование Google Play к приложениям под Android 15+. Выравнивание даёт тулчейн, а не код проекта, поэтому регрессия приезжает молча с обновлением зависимости и всплывает только при заливке в Play Console. |
| `setup-release-signing.ps1` | Создаёт production-keystore и заливает его в GitHub Secrets. Разовая операция; пароль задаёт человек. |

```bash
python tools/ship.py --check                              # проверки без публикации
python tools/ship.py --tag 1.5.0                          # проверки + тег + push = релиз
flutter test tools/preview/render_preview.dart --update-goldens
python tools/check_so_alignment.py build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
```

Про `ship.py` важно: список проверок в нём обязан совпадать со списком шагов в
`.github/workflows/ci.yml`. Разойдутся — локальная проверка перестанет значить
то, что обещает, и это худший вид зелёного результата.

### Выпуск релиза через GitHub Actions

Релизы публикуются автоматически workflow [`.github/workflows/release.yml`](./.github/workflows/release.yml) при пуше тега `v*`.

Шаг 4 ниже лучше заменить одной командой `python tools/ship.py --tag X.Y.Z`: она сначала прогонит те же проверки, что CI, сверит версию в `pubspec.yaml` с тегом и наличие раздела в `CHANGELOG.md` и только потом поставит тег. Ручные команды оставлены, чтобы было видно, что происходит под капотом.

Шаги:

1. Обнови версию в [`pubspec.yaml`](./pubspec.yaml) — например, было `version: 1.0.0+1`, ставим `1.1.0+2` (semver + buildNumber).
2. Опиши изменения в [`CHANGELOG.md`](./CHANGELOG.md) в новой секции `## [1.1.0] - YYYY-MM-DD`. Текст этой секции будет в описании Release.
3. Закоммить и запушь:
   ```powershell
   git commit -am "Release 1.1.0"
   git push
   ```
4. Поставь тег и запушь его:
   ```powershell
   git tag v1.1.0
   git push origin v1.1.0
   ```
5. Через некоторое время странице [Releases](https://github.com/EugeneSoftwareDeveloper/SmartHomeWaterAnalyzer/releases) появится новый релиз с тремя APK.

#### Production-signing через GitHub Secrets (опционально)

По умолчанию APK подписываются debug-keystore'ом, который генерируется в CI на лету. Этого достаточно для распространения через GitHub Releases, но Android при апгрейде требует **ту же** подпись, поэтому debug-keystore не подходит для долгой жизни приложения (CI генерирует разный ключ при каждом запуске).

Для стабильной production-подписи добавь четыре GitHub Secrets в репозитории (`Settings → Secrets and variables → Actions`):

| Secret | Что |
|---|---|
| `ANDROID_KEYSTORE_BASE64` | `base64 -w0 keystore.jks` — твой keystore.jks в base64. На Windows: `[Convert]::ToBase64String([IO.File]::ReadAllBytes("keystore.jks"))` |
| `ANDROID_KEYSTORE_PASSWORD` | Пароль от keystore |
| `ANDROID_KEY_ALIAS` | Имя ключа (например, `water-analyzer`) |
| `ANDROID_KEY_PASSWORD` | Пароль ключа |

Создание keystore (один раз):

```powershell
keytool -genkey -v -keystore water-analyzer-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias water-analyzer
```

После добавления secrets — все следующие релизы будут подписаны production-ключом. **Бэкап keystore сделай немедленно** — потеря ключа значит, что выпускать апгрейды с тем же `applicationId` уже не получится, и пользователям придётся удалить старую версию и поставить новую с нуля.

## Известные ограничения

- **Прибор держит только одно BLE-подключение** — официальное приложение YINMIK блокирует наше. Закрывай его перед использованием.
- **Команды управления спекулятивные** — могут не работать на конкретной партии. См. [docs/03-control-commands.md](./docs/03-control-commands.md) как это починить.
- **Battery percent — оценка** по формуле BLE-YC01 (1950–3190 mV → 0–100%). Для BLE-C600 точная калибровка не подтверждена.
- **Только Android** — Flutter-проект конфигурирован под `--platforms=android`. Архитектура совместима с iOS, но iOS-часть нужно доконфигурировать (Info.plist Bluetooth-разрешения).
- **Фильтр имени может не пропустить экзотическую партию** — ловим `BLE-C600` / `BLE_C600` / `Yinmik-*` / `*C600*` и пакеты с сервисом FF01. Если твой прибор называется как-то ещё, на экране сканирования нажми «Показать все устройства» и выбери вручную; заодно можно расширить `knownNameKeywords` в `lib/yinmik/client.dart`.
- **pH «дрожит»** — электрод стабилизируется во времени, мгновенные значения могут плавать на ±0.1. Это нормально.

## Что планируется

Полный приоритезированный список — в [`docs/06-roadmap.md`](./docs/06-roadmap.md). Топ ближайшего:

- **Реальные байты команд** — заменить спекулятивные `0x08`/`0x10` после подбора через debug-страницу или HCI snoop.
- **Trend indicators ↑↓** — под каждой карточкой замера показывать дельту относительно последнего сохранённого.
- **Серия чтений со стабилизацией pH** — вместо одиночного кадра усреднение по стабилизировавшимся показаниям.
- **Режим сравнения замеров** — multi-select в истории → таблица бок-о-бок.
- **App icon + splash screen** — пока стандартный Flutter-флаг.
- **iOS-сборка** — архитектура совместима, нужны `Info.plist` Bluetooth-разрешения и тестирование.
- **Sentry / Crashlytics**, **фоновый опрос через workmanager**, **несколько устройств с alias-именами**, **golden-тесты** — см. roadmap.

## Документация для разработчиков

- [`AGENTS.md`](./AGENTS.md) — общий профиль проекта, для агента или нового разработчика.
- [`CHANGELOG.md`](./CHANGELOG.md) — история изменений по версиям.
- [`docs/`](./docs/) — техническая документация:
  - [`01-architecture.md`](./docs/01-architecture.md) — слои, точки расширения.
  - [`02-ble-protocol.md`](./docs/02-ble-protocol.md) — GATT-структура, декодер кадра FF02, эталонные кадры.
  - [`03-control-commands.md`](./docs/03-control-commands.md) — HCI snoop guide, как добыть точные байты команд.
  - [`04-ui-design.md`](./docs/04-ui-design.md) — Material 3, виджеты, цветовые конвенции.
  - [`05-state-and-storage.md`](./docs/05-state-and-storage.md) — Riverpod-провайдеры, схема drift и миграции, локализация, навигация.
  - [`06-roadmap.md`](./docs/06-roadmap.md) — приоритезированный список планов.

## Связанный репозиторий

[`SmartHomeService`](https://github.com/EugeneSoftwareDeveloper/SmartHomeService) — .NET 10 сервис умного дома (Windows) с такой же интеграцией BLE-C600 на стороне сервера. Декодер портирован сюда один-в-один, эталонные тестовые кадры общие.

Архитектурно приложения **независимы**: SmartHomeWaterAnalyzer не требует SmartHomeService для работы. Можно использовать параллельно, но не на одном приборе одновременно (один BLE-канал).

## Лицензия

Личный проект. Лицензию определю при первом публичном релизе.
